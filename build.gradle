subprojects {
    apply plugin: 'java'
    apply plugin: 'maven-publish'

    group = 'net.minecraftforge.jdoc'
    java {
        withSourcesJar()
    }

    publishing {
        publications {
            mavenJava(MavenPublication) {
                from components.java
            }
        }
    }
}